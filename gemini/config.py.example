DEBUG = True

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': 'postgres',
        'USER': 'postgres',
        'HOST': 'gemini-db',
        'PORT': 5432,
    }
}

ALLOWED_HOSTS = ['localhost', 'gemini-web']

BASE_DIR = ''

ARCHIVEMATICA = {
  "baseurl": "http://archivematica-storage-service:8000/api/v2/",
  "username": "test",
  "api_key": "test",
  "pipeline_uuid": "b17e28ea-8dd7-4e41-b960-4e7e2851c70a"
}

FEDORA = {
  "baseurl": "http://localhost:8080/fedora/rest/",
  "username": "admin",
  "password": "admin",
}

TMP_DIR = '{}/tmp'.format(BASE_DIR)
TEST_TMP_DIR = '{}/test_tmp'.format(BASE_DIR)

STATIC_ROOT = '/static'
